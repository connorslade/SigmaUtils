package com.connorcode.sigmautils.modules._interface;

import com.connorcode.sigmautils.module.Module;
import com.connorcode.sigmautils.module.ModuleInfo;

@ModuleInfo(description = "Removes the boss bar, leaving only the text",
        documentation = "This is nice on minigame servers that will sometimes use the BossBar to advertise something or just show text information.")
public class NoBossBarValue extends Module {}
